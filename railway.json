{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile.railway"
  },
  "deploy": {
    "restartPolicyType": "always",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}