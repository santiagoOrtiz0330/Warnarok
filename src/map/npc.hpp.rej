diff a/src/map/npc.hpp b/src/map/npc.hpp	(rejected hunks)
@@ -165,7 +165,11 @@ struct npc_data {
 		struct {
 			struct script_code *script;
 			short xs,ys; // OnTouch area radius
+#ifdef BGEXTENDED
+			int guild_id, bg_id;
+#else
 			int guild_id;
+#endif
 			t_tick timer;
 			int timerid,timeramount,rid;
 			t_tick timertick;
